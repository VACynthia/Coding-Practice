#include <stdio.h>

/**
 * main - addresses of variables
 *
 * Return: always 0
 */

int main(void)
{
	char k;
	int p;

	printf("Address of variable 'k': %p\n", &k);
	printf("Address of variable 'p': %p\n", &p);

	return (0);
}
